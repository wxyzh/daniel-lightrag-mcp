# LightRAG MCP Server Configuration

# LightRAG Server Configuration
LIGHTRAG_BASE_URL=http://localhost:9621
LIGHTRAG_API_KEY=
LIGHTRAG_TIMEOUT=30

# Logging Configuration
LOG_LEVEL=INFO

# Tool Prefix Configuration (Optional)
# Use this to differentiate multiple MCP server instances
# Examples:
#   - For novel writing style queries: LIGHTRAG_TOOL_PREFIX=novel_style_
#   - For novel content queries: LIGHTRAG_TOOL_PREFIX=novel_content_
#   - For research queries: LIGHTRAG_TOOL_PREFIX=research_
# When set, all tool names will be prefixed (e.g., "novel_style_query_text")
# and descriptions will show the prefix in brackets (e.g., "[novel_style] Query LightRAG with text")
LIGHTRAG_TOOL_PREFIX=

# HTTP Server Configuration (Optional)
# Host to bind the HTTP server (default: 127.0.0.1)
# Use 0.0.0.0 to allow external connections
LIGHTRAG_HTTP_HOST=127.0.0.1

# Port to bind the HTTP server (default: 8765)
# Change this if port 8765 is already in use
LIGHTRAG_HTTP_PORT=8765

# Multiple HTTP Server Instances Configuration
# Format: prefix1:url1:api_key1,prefix2:url2:api_key2
# Example:
#   LIGHTRAG_HTTP_PREFIXES=novel_style:http://localhost:9621:key1,novel_content:http://localhost:9622:key2
LIGHTRAG_HTTP_PREFIXES=
